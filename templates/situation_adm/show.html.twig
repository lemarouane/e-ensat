{% extends 'base.html.twig' %}

{% block title %}SituationAdm{% endblock %}

{% block body %}
    <h1>SituationAdm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ situation_adm.id }}</td>
            </tr>
            <tr>
                <th>LibelleSituation</th>
                <td>{{ situation_adm.libelleSituation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_situation_adm_index') }}">back to list</a>

    <a href="{{ path('app_situation_adm_edit', {'id': situation_adm.id}) }}">edit</a>

    {{ include('situation_adm/_delete_form.html.twig') }}
{% endblock %}
