
{% extends 'template.html.twig' %}

{% block title %}{{'liste_prof_projet'|trans}} {% endblock %}

{% block body %}

				<!--breadcrumb-->

                <div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
                        <div class="breadcrumb-title pe-3">{{'finance'|trans}}</div>
                        <div class="ps-3">
                            <nav aria-label="breadcrumb">
                            <ol class="breadcrumb mb-0 p-0">
                                <li class="breadcrumb-item"><a href="javascript:;"><i class="bi bi-cash-coin"></i></a>
                                </li>
                                <li class="breadcrumb-item active" aria-current="page"> {{'liste_prof_projet'|trans}}</li>
                            </ol>
                            </nav>
                        </div>
         
                </div>
                <hr>

                <div class="card border-top border-success border-0 border-3">
                  <div class="card-body">
                    <div class="table-responsive">
                      <table id="exe1" class="table table-striped table-bordered dataTable" style="text-align:center !important ;" >
                        <thead>
                          <tr>
                                <th style="text-align:center !important ;">{{'nom'|trans}}</th>
                                <th style="text-align:center !important ;">{{'prenom'|trans}}</th>
                                <th style="text-align:center !important ;">{{'cin'|trans}}</th>
                                <th style="text-align:center !important ;">{{'op'|trans}}</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for liste_prof in liste_profs %}
                                <tr>
                                    <td>{{liste_prof.nom}}</td>
                                    <td>{{liste_prof.prenom}}</td>
                                    <td>{{liste_prof.cin}}</td>
                                    <td>
                                        <div class="text-center">
                        
                                            <a class="text-primary icons" data-toggle="tooltip" data-placement="top" title="ajouter" href="{{ path('addPaiementprojet', { 'id': liste_prof.id }) }}"><span class="bi bi-credit-card-fill"></span></a>&nbsp;&nbsp;

                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>

							
					</table>
					</div>
                    
					</div>
				</div>
  
{% endblock %}
{% block javascripts %}
    {{parent()}}
	
    {{ encore_entry_script_tags('scolarite') }}
        


{% endblock %}

