<form method="post" action="{{ path('app_paiementprojet_delete', {'id': paiementprojet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiementprojet.id) }}">
    <button class="btn">Delete</button>
</form>
