<form method="post" action="{{ path('app_params_delete', {'id': param.id}) }}" onsubmit="return confirm({{'Are youss sure you want to delete this item?'}});">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.id) }}">
    <button class="btn">Delete</button>
</form>
