<form method="post" action="{{ path('app_budget_sortie_delete', {'id': budget_sortie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ budget_sortie.id) }}">
    <button class="btn">Delete</button>
</form>
