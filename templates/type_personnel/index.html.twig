{% extends 'base.html.twig' %}

{% block title %}TypePersonnel index{% endblock %}

{% block body %}
    <h1>TypePersonnel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>LibellePersonnel</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_personnel in type_personnels %}
            <tr>
                <td>{{ type_personnel.id }}</td>
                <td>{{ type_personnel.libellePersonnel }}</td>
                <td>
                    <a href="{{ path('app_type_personnel_show', {'id': type_personnel.id}) }}">show</a>
                    <a href="{{ path('app_type_personnel_edit', {'id': type_personnel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_personnel_new') }}">Create new</a>
{% endblock %}
