<form method="post" action="{{ path('app_echelon_av_delete', {'id': echelon_av.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ echelon_av.id) }}">
    <button class="btn">Delete</button>
</form>
