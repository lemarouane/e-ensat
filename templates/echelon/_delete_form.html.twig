<form method="post" action="{{ path('app_echelon_delete', {'id': echelon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ echelon.id) }}">
    <button class="btn">Delete</button>
</form>
