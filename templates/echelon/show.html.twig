{% extends 'base.html.twig' %}

{% block title %}Echelon{% endblock %}

{% block body %}
    <h1>Echelon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ echelon.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ echelon.designation }}</td>
            </tr>
            <tr>
                <th>NbAnnee</th>
                <td>{{ echelon.nbAnnee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_echelon_index') }}">back to list</a>

    <a href="{{ path('app_echelon_edit', {'id': echelon.id}) }}">edit</a>

    {{ include('echelon/_delete_form.html.twig') }}
{% endblock %}
