<form method="post" action="{{ path('app_programme_emploi_restant_delete', {'id': programme_emploi_restant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme_emploi_restant.id) }}">
    <button class="btn">Delete</button>
</form>
