{% extends "template.html.twig" %}

{% block title %}{{'articlesPE'|trans}}{% endblock %}

{% block body %}
 <!--breadcrumb-->
        <div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
          <div class="breadcrumb-title pe-3">{{'articlesPE'|trans}}</div>
          <div class="ps-3">
            <nav aria-label="breadcrumb">
              <ol class="breadcrumb mb-0 p-0">
                <li class="breadcrumb-item"><a href="javascript:;"><i class="bx bx-home-alt"></i></a>
                </li>
                <li class="breadcrumb-item active" aria-current="page"> {{'details_paragraphe_n'|trans}}  {{paragraphe.id}}  </li>
              </ol>
            </nav>
          </div>
        
        </div>
        <hr/>

        <div class="card">
          <div class="card-body">
            <div class="table-responsive">
              

					<table class="table table-striped table-bordered" >
							
								<tbody>
					
										<tr>
											<td> {{'intitule_articlePE'|trans}}</td> <td>{{paragraphe.articlePE.libelle}}</td>
										</tr>
										<tr>
											<td>{{'n_paragraphe'|trans}}</td> <td>{{paragraphe.id}}</td>
										</tr>
					
				            <tr>
											<td> {{'libelle_paragraphe'|trans}}</td> <td>{{paragraphe.libelle}}</td>
										</tr>

										<tr>
											<td> {{'n_paragraphe'|trans}}</td> <td>{{paragraphe.numParagraphe}}</td>
										</tr>

										

										
		
								</tbody>
							
							</table>

            </div>


          </div>
        </div>
        <div>                              
          <a class="btn btn btn-primary px-4 col-2" style="float:left" href="{{ path('paragraphes') }}"> {{'back'|trans}} </a>
        </div>
							                    

{% endblock %}
{% block javascripts %}
	{{ parent() }}
{% endblock %}

							              	
              							