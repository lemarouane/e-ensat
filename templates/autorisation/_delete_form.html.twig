<form method="post" action="{{ path('app_autorisation_delete', {'id': autorisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autorisation.id) }}">
    <button class="btn">Delete</button>
</form>
