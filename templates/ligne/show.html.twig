{% extends "template.html.twig" %}

{% block title %}{{'lignes'|trans}}{% endblock %}

{% block body %}
 <!--breadcrumb-->
        <div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
          <div class="breadcrumb-title pe-3">{{'lignes'|trans}}</div>
          <div class="ps-3">
            <nav aria-label="breadcrumb">
              <ol class="breadcrumb mb-0 p-0">
                <li class="breadcrumb-item"><a href="javascript:;"><i class="bx bx-home-alt"></i></a>
                </li>
                <li class="breadcrumb-item active" aria-current="page"> {{'details_ligne_n'|trans}}  {{ligne.id}}  </li>
              </ol>
            </nav>
          </div>
        
        </div>
        <hr/>

        <div class="card">
          <div class="card-body">
            <div class="table-responsive">
              

					<table class="table table-striped table-bordered" >
							
								<tbody>
					
										<tr>
											<td> {{'intitule_articlePE'|trans}}</td> <td>{{ligne.articlePE.libelle}}</td>
										</tr>
										<tr>
											<td> {{'intitule_paragraphe'|trans}}</td> <td>{{ligne.paragraphe.libelle}}</td>
										</tr>
										<tr>
											<td>{{'n'|trans}}</td> <td>{{ligne.id}}</td>
										</tr>
					
				                       	<tr>
											<td> {{'intitule_ligne'|trans}}</td> <td>{{ligne.libelle}}</td>
										</tr>

										<tr>
											<td> {{'n_ligne'|trans}}</td> <td>{{ligne.numLigne}}</td>
										</tr>
										<tr>
											<td>{{'type'|trans}}</td> <td>{{ligne.type}}</td>
										</tr>

										
		
								</tbody>
							
							</table>

            </div>


          </div>
        </div>
		<div>
        	<a class="btn btn btn-primary px-4 col-2" style="float:left" href="{{ path('lignes') }}"> {{'back'|trans}} </a>                     
        </div>
							                    

{% endblock %}
{% block javascripts %}
	{{ parent() }}
{% endblock %}

              						