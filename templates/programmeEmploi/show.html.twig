{% extends "template.html.twig" %}
{% block title %}
{{'programmeEmploi_FC_prof'|trans}}{{ parent() }}
{% endblock %}

{% block body %}

	<div class="page-breadcrumb d-none d-sm-flex align-items-center mb-3">
					<div class="breadcrumb-title pe-3">{{'programmeEmploi_FC_prof'|trans}} </div>
					<div class="ps-3">
						<nav aria-label="breadcrumb">
							<ol class="breadcrumb mb-0 p-0">
								<li class="breadcrumb-item"><a href="javascript:;"><i class="bi bi-cash-coin"></i></a>
								</li>
								<li class="breadcrumb-item active" aria-current="page">{{'programme_emploi_detail'|trans}} {{ programmeEmploi.id}}</li>
							</ol>
						</nav>
					</div>
				</div>
		<hr/>







      <div class="card">
          <div class="card-body">
            <div class="table-responsive">
              

					<table class="table table-striped table-bordered" >
							
								<tbody>
										<tr>
											<td>{{'ref'|trans}}</td> <td>  {{ programmeEmploi.reference}}</td>
										</tr>
					
										<tr>
											<td> {{'intitule'|trans}}</td> <td>{{programmeEmploi.intitule}}</td>
										</tr>
										<tr>
											<td> {{'annee'|trans}}</td> <td>{{programmeEmploi.annee}}</td>
										</tr>
										<tr>
											<td> {{'montant'|trans}}</td> <td style="color:green;font-weight:bold;">  {{programmeEmploi.montant|number_format(2, ',', '.')}} {{'dh'|trans}}    </td>
										</tr>
										
					
				            <tr>
											<td> {{'type'|trans}}</td> <td>
                      
                          {% if programmeEmploi.type == '1' %}
                                        {{'recette_propre'|trans}}
                                    {% elseif programmeEmploi.type == '2' %}
                                        {{'sub_etat'|trans}}
                                    {% endif %}
                      </td>
										</tr>

										<tr>
											<td> {{'personnel'|trans}}</td> <td> {{ programmeEmploi.personne.nom}} {{ programmeEmploi.personne.prenom }}</td>
										</tr>

										
		
								</tbody>
							
							</table>

            </div>


          </div>
        </div>




    <section class="invoice">

    
        
      
<div class="card shadow-none bg-light border">
      <div class="card-body">
      <div class="card-header py-2 bg-transparent"><h6 class="mb-0"></h6>{{'liste_rubriques'|trans}}</div>
             <div class="row g-3">
                        <div class="table-responsive">
                              <table id="example7" class="table table-bordered table-hover dataTable" role="grid" aria-describedby="example2_info" style="text-align:center !important ;">
                                <thead>
                                  <tr role="row">

                                  <th class="sorting" tabindex="0" aria-controls="example2" rowspan="1" colspan="1" aria-sort="ascending" aria-label="article: activate to sort column descending" style="text-align:center !important ; ">{{'Rubrique'|trans}}</th>
                                  <th class="sorting" tabindex="0" aria-controls="example2" rowspan="1" colspan="1" aria-label="quantité: activate to sort column ascending" style="text-align:center !important ; ">{{'Montant'|trans}}</th> 
                                    
                                  </tr>
                                </thead>
                                <tbody>
                                {% for programmeElement in programmeEmploi.element %}
                                  <tr style="font-weight:bold;" role="row" class="odd">
                                    <td class="sorting_1">{{ programmeElement.rubrique.libelle }}</td>
                                    <td style="color:green;"> {{programmeElement.montant|number_format(2, ',', '.')}} {{'dh'|trans}}   </td>
                                  </tr>
                                {% endfor %}
                              </tbody>
                            </table>
                          </div>
                        </div>
                    </div>
            <div>
                             
              
    </section>


{% endblock body %}

 {% block javascripts %}
{{ parent() }}
<script> 
    (function($) {
    })(jQuery);
</script>

{% endblock %}