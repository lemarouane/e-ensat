{% extends 'base.html.twig' %}

{% block title %}StructRech{% endblock %}

{% block body %}
    <h1>StructRech</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ struct_rech.id }}</td>
            </tr>
            <tr>
                <th>LibelleStructure</th>
                <td>{{ struct_rech.libelleStructure }}</td>
            </tr>
            <tr>
                <th>AbrevStructure</th>
                <td>{{ struct_rech.abrevStructure }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_struct_rech_index') }}">back to list</a>

    <a href="{{ path('app_struct_rech_edit', {'id': struct_rech.id}) }}">edit</a>

    {{ include('struct_rech/_delete_form.html.twig') }}
{% endblock %}
