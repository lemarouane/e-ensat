<form method="post" action="{{ path('app_struct_rech_delete', {'id': struct_rech.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ struct_rech.id) }}">
    <button class="btn">Delete</button>
</form>
