<form method="post" action="{{ path('app_programme_emploi_projet_delete', {'id': programme_emploi_projet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programme_emploi_projet.id) }}">
    <button class="btn">Delete</button>
</form>
